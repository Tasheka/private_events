<h1> Invite </h1>

      <%= form_with model: @invitation, method: :post do |f| %>
        Enter a user's email: <%= text_field_tag :email %>
        <%= hidden_field_tag 'event_id', @event  %>
         <%= f.submit 'Invite' %>
      <% end %>   
      <br />
